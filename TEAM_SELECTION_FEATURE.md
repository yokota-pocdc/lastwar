# チームA/B個別選択機能

## 概要

チームAとチームBは別の日時で開催されるため、ユーザーはどちらか一方のチームのみを選択して申し込むことができます。

## 主な変更点

### 1. ユーザー側の変更

#### イベント詳細モーダル
- **チーム選択ボタン**: 参加申し込み時にチームAまたはチームBを選択
- **制約**: 両方のチームに同時申し込みは不可
- **表示**: 希望チームが申込内容に表示される

#### サイコロ画面
- 選択したチーム名が表示される
- 振り直し時も同じチームで継続

### 2. 抽選ロジックの変更

#### 旧ロジック（修正前）
- `preferred_team`が'any'の場合、空いているチームに自動振り分け
- チームAが満員の場合、チームBに回される

#### 新ロジック（修正後）
- ユーザーが選択したチームにのみ割り当て
- 選択したチームが満員の場合は**落選**
- `preferred_team`は'A'または'B'のみ（'any'は廃止）

### 3. 管理画面の変更

#### 結果表示テーブル
- **希望チーム列**を追加
  - チームA: 青いバッジ
  - チームB: 紫のバッジ
- **結果チーム列**と**希望チーム列**を分離

#### CSV出力
- 「希望チーム」列を追加
- 希望チームと結果チームの両方を出力

---

## 使用方法

### ユーザー側

1. **イベントを選択**
   - カレンダーからイベントをクリック

2. **チームを選択**
   - 「チームA」または「チームB」ボタンをクリック
   - use_team_b=0の場合、チームAのみ表示

3. **サイコロを振る**
   - 選択したチームに申し込む
   - チケット使用の有無を選択

4. **結果確認**
   - 希望チーム、サイコロ出目、合計スコアが表示
   - 抽選後、参加者/候補者/落選が表示

### 管理者側

1. **抽選実行**
   - チームAを希望した人→チームAにのみ割り当て
   - チームBを希望した人→チームBにのみ割り当て
   - 満員の場合は落選

2. **結果確認**
   - 「希望」列で各ユーザーの希望チームを確認
   - 「結果」列で実際に割り当てられたチームを確認
   - 落選者は希望チームが表示されるが結果チームは空

3. **CSV出力**
   - 希望チームと結果チームの両方が含まれる

---

## 技術的な詳細

### データベース
- `applications.preferred_team`: 'A' | 'B'（必須）
- 'any'は使用しない

### 抽選アルゴリム
```typescript
// チームAを希望した場合
if (app.preferred_team === 'A') {
  if (チームA参加者枠に空きあり) {
    → チームA参加者に割り当て
  } else if (チームA候補者枠に空きあり) {
    → チームA候補者に割り当て
  } else {
    → 落選
  }
}

// チームBを希望した場合（同様）
if (app.preferred_team === 'B' && use_team_b) {
  ...
}
```

### 落選条件
- 選択したチームの定員（参加者+候補者）を超えた場合
- スコアが低い順に落選

---

## 変更ファイル

1. `components/EventModal.tsx`
   - チーム選択ボタンを追加
   - 希望チーム表示を追加

2. `components/DiceRoller.tsx`
   - `selectedTeam` propsを追加
   - チーム名表示を追加

3. `lib/lottery.ts`
   - `Application.preferred_team`から'any'を削除
   - 抽選ロジックを修正

4. `app/admin/results/[id]/page.tsx`
   - 結果テーブルに希望チーム列を追加
   - CSV出力に希望チームを追加

---

## 注意事項

- **既存データとの互換性**: 既存の申込データで`preferred_team='any'`のデータがある場合、エラーになる可能性があります。データベースのクリーンアップが必要です。

- **use_team_b=0の場合**: チームBボタンは表示されません。チームAのみ選択可能です。

---

## 今後の拡張案

1. **チーム別の定員設定**
   - チームAとチームBで異なる定員を設定

2. **チーム別の開催日時表示**
   - イベント作成時にチームAとチームBの開催日時を別々に設定
   - 選択画面で各チームの日時を表示

3. **希望チーム変更機能**
   - 抽選前であれば希望チームを変更可能に
